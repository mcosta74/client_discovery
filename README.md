# Client Discovery Demo

## Overview

In this PoC we implement the following algorithm:

- look for a service instance in the local network
- if the service exists, use it
- if no instances are found, promote itself as service

## Build

## Usage